@model thn.Models.profile

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>org</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.about)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.about)
            @Html.ValidationMessageFor(model => model.about)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.searchRadius)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.searchRadius)
            @Html.ValidationMessageFor(model => model.searchRadius)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.tags)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.tags)
            @Html.ValidationMessageFor(model => model.tags)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
